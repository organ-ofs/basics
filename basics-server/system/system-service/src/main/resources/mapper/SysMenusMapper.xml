<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ofs.sys.mapper.SysMenusMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ofs.sys.entity.SysMenus">
        <result column="ID" property="id"/>
        <result column="NAME" property="name"/>
        <result column="RESOURCE_ID" property="resourceId"/>
        <result column="PARENT_ID" property="parentId"/>
        <result column="GRADE" property="grade"/>
        <result column="PATH" property="path"/>
        <result column="COMPONENT" property="component"/>
        <result column="TITLE" property="title"/>
        <result column="PERMISSION" property="permission"/>
        <result column="SORT" property="sort"/>
        <result column="ICON" property="icon"/>
        <result column="HIDDEN" property="hidden"/>
        <result column="VERIFICATION" property="verification"/>
        <result column="DESCRIPTION" property="description"/>
        <result column="CREATE_DATE" property="createDate"/>
        <result column="UPDATE_DATE" property="updateDate"/>
        <result column="CREATE_USER" property="createUser"/>
        <result column="UPDATE_USER" property="updateUser"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        T.ID, T.NAME, T.RESOURCE_ID, T.PARENT_ID, T.GRADE, T.PATH, T.COMPONENT, T.TITLE, T.PERMISSION,
        T.SORT, T.ICON, T.HIDDEN, T.VERIFICATION, T.DESCRIPTION,
        T.CREATE_DATE, T.UPDATE_DATE, T.CREATE_USER, T.UPDATE_USER
    </sql>

    <sql id="where">
        <where>
            <if test="params.title!=null and params.title!=''">T.TITLE=#{params.title}</if>
        </where>
    </sql>
    <select id="listPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_menus T
        <include refid="where"/>
    </select>
</mapper>
